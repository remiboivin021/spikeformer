# Model Context

Not applicable — this project does not use external AI models (LLM, GPT, etc.).
This is a PyTorch/SpikingJelly machine learning project that trains SNN/ANN models locally.
Inference is done via PyTorch or exported ONNX.
